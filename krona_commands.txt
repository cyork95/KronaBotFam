>krona_load <extension>     #This takes a cog and loads it to use the commands of that cog.

>krona_unload <extension>   #This takes a cog and unloads it so it can no longer be used.

>krona_reload <extension>   #This refreshes a cog if there was a code change.

>krona_ping     #Returns the bot latency

>lazy   #Returns a message to the lazy discord people

>clear <amount>     #Deletes the specified number of chats. Default is 2 messages. (admin)

>kick <member> [reason]	    #Kicks the specified member for an optional reason. (admin)

>ban <member> [reason]  #Bans the specified member for an optional reason. (admin)

>unban <member>	    #Unbans the member (admin)

>mute <member>  #Mutes the member. (admin)

>unmute <member>    #Unmutes the member. (admin)

>add_role <member> <role>   #Adds a role to the specified user. (admin)

>remove_role <member> <role>    #Removes a role from the specified user. (admin)

>get_recipe	    #Gets a random recipe. (recipe)

>random_cat     #Gets a random cat picture. (cat)

